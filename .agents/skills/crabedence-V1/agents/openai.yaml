interface:
  display_name: "Crabedence V1"
  short_description: "Repo-specific patterns and workflows for crabedence-V1"
  default_prompt: "Use the crabedence-V1 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true